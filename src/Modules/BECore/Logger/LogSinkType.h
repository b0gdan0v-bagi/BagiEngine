#pragma once

#include <BECore/Utils/EnumUtils.h>

namespace BECore {

    /**
     * @brief Enum defining available log sink types
     */
    CORE_ENUM(LogSinkType, uint8_t, Console, File, Output)

}  // namespace BECore
