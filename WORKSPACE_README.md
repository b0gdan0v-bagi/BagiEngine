# BagiEngine Workspace - Быстрый запуск

Этот проект включает готовые скрипты для быстрого открытия workspace в Cursor на любой платформе.

## 🚀 Запуск на Windows

### Вариант 1: CMD (Командная строка)
Просто дважды кликните на файл `start.cmd` или выполните в командной строке:
```cmd
start.cmd
```

### Вариант 2: PowerShell
Дважды кликните на файл `start.ps1` или выполните в PowerShell:
```powershell
.\start.ps1
```

**Примечание:** Если PowerShell блокирует выполнение скрипта, выполните один раз:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 🍎 Запуск на macOS

### Вариант 1: Двойной клик (рекомендуется)
Дважды кликните на файл **`start.command`** - macOS автоматически откроет его в Terminal и запустит.

### Вариант 2: Через терминал
Выполните в терминале:
```bash
./start.sh
```
или
```bash
./start.command
```

**Примечание:** Файл `start.sh` не запускается двойным кликом на macOS. Используйте `start.command` для запуска двойным кликом.

## 🐧 Запуск на Linux

Выполните в терминале:
```bash
./start.sh
```

## 📝 Альтернативный способ (все платформы)

Если у вас `cursor` добавлен в PATH:

**Windows:**
```cmd
cursor BagiEngine.code-workspace
```

**macOS/Linux:**
```bash
cursor BagiEngine.code-workspace
```

## ⚙️ Что делают скрипты?

Скрипты автоматически:
1. Ищут Cursor в стандартных путях установки
2. Открывают файл `BagiEngine.code-workspace` в Cursor
3. Если Cursor не найден - открывают workspace приложением по умолчанию

## 🔧 Настройка проекта после открытия

После открытия workspace:
1. Cursor автоматически загрузит все настройки
2. Рекомендуемые расширения будут предложены к установке
3. IntelliSense будет настроен для C++20

### Сборка проекта:
- **Ctrl/Cmd + Shift + B** - собрать проект
- **F5** - запустить отладку (выберите конфигурацию для вашей ОС)

### Задачи CMake:
- `CMake: configure` - настроить проект (Ninja)
- `CMake: configure (MSVC)` - настроить для Visual Studio (только Windows)
- `CMake: build` - собрать проект
- `Run MyGame` - запустить игру

---

Приятной разработки! 🎮✨
